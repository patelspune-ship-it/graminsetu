DATABASE_URL=postgresql+psycopg://graminsetu:graminsetu_dev@localhost:5433/graminsetu

# Optional. Voice (Bhashini ULCA pipeline) is disabled without these; the
# frontend falls back to the browser Web Speech API when unset.
BHASHINI_API_KEY=
BHASHINI_USER_ID=
